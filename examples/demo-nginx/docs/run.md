# Running

- Deploy via `kubectl apply -f k8s/demo-workload.yaml`
- Confirm pods in namespace `backstage-demo` with `kubectl get pods -n backstage-demo`

TechDocs should be available from the catalog component page after successful build.
